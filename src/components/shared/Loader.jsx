import React from 'react';

const Loader = () => {
  return (
    <section className="loader">
      <div className="container">Soy loader</div>
    </section>
  );
};

export default Loader;
